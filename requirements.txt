# ============================================================
# requirements.txt — DINO-AugSeg
# Môi trường: Python >= 3.8
# Cài đặt: pip install -r requirements.txt
# ============================================================

# ---- Core Deep Learning ----
torch>=2.0.0
torchvision>=0.15.0

# ---- Wavelet Transform (bắt buộc cho WT-Aug) ----
pytorch-wavelets>=1.3.0
PyWavelets>=1.4.0          # backend của pytorch-wavelets

# ---- Image Processing ----
Pillow>=9.0.0
numpy>=1.23.0
opencv-python>=4.7.0       # tùy chọn, dùng nếu cần xử lý ảnh ngoài PIL

# ---- Metrics (tùy chọn, dùng cho HD95 trong eval_custom.py) ----
medpy>=0.4.0               # pip install medpy  (cần SimpleITK)

# ---- Utilities ----
tqdm>=4.64.0               # progress bar khi cần
matplotlib>=3.6.0          # visualize kết quả / debug
